<!DOCTYPE html>
<html lang="th">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>MK1 Game</title>

<link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@700&display=swap" rel="stylesheet">

<style>
*{
    margin:0;
    padding:0;
    box-sizing:border-box;
    font-family:'Orbitron',sans-serif;
}

body{
    height:100vh;
    background:url('bg.jpg') no-repeat center center/cover;
    display:flex;
    justify-content:center;
    align-items:center;
    overflow:hidden;
    position:relative;
}

/* เลเยอร์มืด */
body::before{
    content:"";
    position:absolute;
    width:100%;
    height:100%;
    background:linear-gradient(135deg,rgba(0,0,0,0.85),rgba(255,0,120,0.4));
    z-index:0;
}

/* กล่อง */
.container{
    position:relative;
    z-index:2;
    text-align:center;
    background:rgba(0,0,0,0.7);
    padding:50px;
    border-radius:20px;
    box-shadow:0 0 40px #ff0080;
    backdrop-filter:blur(10px);
}

/* ชื่อเลื่อนเข้าจอ */
.game-title{
    font-size:60px;
    color:#ff2fa0;
    text-shadow:0 0 15px #ff0080,0 0 40px #ff0080;
    position:relative;
    animation:slideIn 1.5s ease-out forwards;
    opacity:0;
}

/* เอฟเฟกต์ Slide */
@keyframes slideIn{
    0%{
        transform:translateX(-100vw);
        opacity:0;
    }
    70%{
        transform:translateX(20px);
        opacity:1;
    }
    100%{
        transform:translateX(0);
        opacity:1;
    }
}

/* เอฟเฟกต์ Glitch */
.game-title::before,
.game-title::after{
    content:attr(data-text);
    position:absolute;
    left:0;
    width:100%;
    overflow:hidden;
}

.game-title::before{
    animation:glitchTop 2s infinite linear alternate-reverse;
    color:#00ffff;
    clip-path:polygon(0 0,100% 0,100% 45%,0 45%);
}

.game-title::after{
    animation:glitchBottom 2s infinite linear alternate-reverse;
    color:#ff00ff;
    clip-path:polygon(0 55%,100% 55%,100% 100%,0 100%);
}

@keyframes glitchTop{
    0%{transform:translate(0);}
    20%{transform:translate(-3px,-3px);}
    40%{transform:translate(3px,3px);}
    60%{transform:translate(-3px,3px);}
    80%{transform:translate(3px,-3px);}
    100%{transform:translate(0);}
}

@keyframes glitchBottom{
    0%{transform:translate(0);}
    20%{transform:translate(3px,3px);}
    40%{transform:translate(-3px,-3px);}
    60%{transform:translate(3px,-3px);}
    80%{transform:translate(-3px,3px);}
    100%{transform:translate(0);}
}

/* ปุ่ม */
.btn{
    display:inline-block;
    margin:15px;
    padding:15px 30px;
    border-radius:50px;
    text-decoration:none;
    font-size:18px;
    font-weight:bold;
    color:white;
    transition:0.3s;
}

.facebook{
    background:linear-gradient(45deg,#1877f2,#00c6ff);
}

.instagram{
    background:linear-gradient(45deg,#feda75,#d62976,#962fbf,#4f5bd5);
}

.btn:hover{
    transform:scale(1.1);
    box-shadow:0 0 25px white;
}
</style>
</head>

<body>

<div class="container">

<h1 class="game-title" data-text="MK1 Game">
    MK1 Game
</h1>

<br>

<a class="btn facebook" href="https://www.facebook.com/share/1B5c7GuPdh/" target="_blank">
    Facebook
</a>

<a class="btn instagram" href="https://www.instagram.com/non_pim1?igsh=MXRrazNkcGl5eWx6Ng==" target="_blank">
    Instagram
</a>

</div>

</body>
</html>
